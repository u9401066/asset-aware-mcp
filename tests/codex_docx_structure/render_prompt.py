"""Explicit real image review instructions, separate from native structure proof."""

INSTRUCTIONS = """4b. Discover render_docx_page. VIEW every page of the final DOCX and every page
    of the historical revision where the Count was temporarily changed to '008'.
    Start docx_page_index=0 at each explicit revision; follow next_page_index until
    null, retaining renderer identity and returned page count. Use render_size=1400.
    Compare visible strings, merged title, grid, clipping, page flow, Chinese heading
    glyphs and source scan. Native DFM alone is not a visual review. Report actual
    differences/uncertainties; never claim you used Microsoft Word.
"""
